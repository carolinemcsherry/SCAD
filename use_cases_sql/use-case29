SELECT A.Name AS cityname,
       B.Name AS countryname,
       B.Continent,
       A.Population
FROM city A
LEFT JOIN country B ON A.CountryCode = B.Code;

-- retrieves city, country, continent and population